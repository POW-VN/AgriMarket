import React from 'react';
import './Login.css'; // Import file CSS của bạn

const Login = () => {
    return (
        // Dán phần mã JSX (thẻ div, form, input...) từ Figma vào đây
        <div className="login-container">
            {/* ... */}
        </div>
    );
};

export default Login;